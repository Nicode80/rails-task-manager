<h1><%= @task.title %></h1>

<div>
  <%= @task.details %>
</div>

<div>
  <% if @task.completed %>
    <p> ☑ <%= @task.title %></p>
  <% else %>
    <p> ☐ <%= @task.title %></p>
  <% end %>
</div>

<%= link_to 'Back to list', tasks_path %>
